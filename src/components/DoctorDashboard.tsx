/**
 * DELETED IN THE REFACTOR — this file is safe to remove.
 *
 * The 1,100-line Figma Make export that lived here was split into:
 *   src/pages/doctor/*       Queue, Clinical Summary, Timeline, Alerts
 *   src/components/doctor/*  DoctorSidebar, PatientRow, TimelineItem, AlertCard…
 *   src/layouts/DoctorLayout sidebar, top bar and shared patient selection
 *   src/data/*               patients, histories, timeline and alerts
 *
 * Only an empty module is left so that a stale import somewhere cannot break
 * the build before you delete the file.
 */
export {}
