/**
 * DELETED IN THE REFACTOR — this file is safe to remove.
 *
 * The 1,400-line Figma Make export that lived here was split into:
 *   src/pages/patient/*      one file per screen
 *   src/components/patient/* MicButton, VoiceInput, QuestionCard, DocumentCard…
 *   src/components/common/*  Button, Card, ProgressBar, AudioButton…
 *   src/layouts/KioskLayout  header, progress and footer chrome
 *   src/data/questions.ts    the adaptive question bank
 *
 * Only an empty module is left so that a stale import somewhere cannot break
 * the build before you delete the file.
 */
export {}
